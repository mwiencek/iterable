// @flow

declare function foldl<T, U>(
  func: (U, T) => U,
): (accum: U) => (iterable: Iterable<T>) => U;

declare function foldl<T, U>(
  func: (U, T) => U,
  accum: U,
): (iterable: Iterable<T>) => U;

declare export default typeof foldl;
