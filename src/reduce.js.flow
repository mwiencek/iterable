// @flow

declare function reduce<T, U>(
  func: (U, T) => U,
): (accum: U) => (iterable: Iterable<T>) => U;

declare function reduce<T, U>(
  func: (U, T) => U,
  accum: U,
): (iterable: Iterable<T>) => U;

declare export default typeof reduce;
